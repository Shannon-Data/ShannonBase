to hold the 3rd party libs used by rapid.

1:onnxruntime, onnx runtime libs
2:objectstore, to access object storage, such as to access s3(aws), obs(huawe cloud), etc.
  rep: c++,  https://github.com/aws/aws-sdk-cpp
