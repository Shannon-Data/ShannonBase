# Local overrides for the bench scripts (git-ignored).
# Generated for this dev box: server at 127.0.0.1:3306, client not on PATH.
MYSQL_CLIENT="/home/lihao/workshop/shannon-bin/bin/mysql"
MYSQL_HOST="${MYSQL_HOST:-127.0.0.1}"
MYSQL_PORT="${MYSQL_PORT:-3306}"
MYSQL_USER="${MYSQL_USER:-root}"
MYSQL_PASS="${MYSQL_PASS:-123456}"
